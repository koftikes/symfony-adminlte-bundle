{% import "@SbSAdminLTE/SideBar/menu_macros.html.twig" as admin_lte_macros %}
<ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu">
    {% for item in menu %}
        {{ admin_lte_macros.render_menu_item(item) }}
    {% endfor %}
</ul>
